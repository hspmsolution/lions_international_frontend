import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
    activitiesCont: {
        background: '#112E57'
    },
    activeH: {
        color: 'white',
        textAlign: 'center'
    }
}))
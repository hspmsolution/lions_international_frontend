import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
    rankRow: {
        display: 'flex'
    },
    rankColumn: {
        minWidth: '50%'
    },
    teamSubH: {
        fontSize: '35px',
        fontFamily: "'Aboreto', cursive",
        fontWeight: 'bolder',
        textAlign: 'center',
    }
}))
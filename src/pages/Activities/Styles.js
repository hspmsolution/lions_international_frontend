import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    activityContainer: {
        margin: "3rem auto",
    },
    eventCard: {
        padding: 0,
        textAlign: 'center'
    }
}))